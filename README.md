# li_test_repo

Task Board demo for team members to create, assign, and update tasks with real-time sync.

Run locally:
- npm install
- npm start

Open http://localhost:3000 and use the board. Drag tasks between columns and see updates live across open tabs.